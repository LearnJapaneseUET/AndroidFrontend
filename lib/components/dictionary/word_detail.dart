import 'package:flutter/material.dart';

class WordDetail extends StatelessWidget {
  final String? word;
  const WordDetail ({super.key, this.word});

  @override
  Widget build(BuildContext context) {
    return const Expanded(
      child: Column(
        children: [
          Row(
            // Từ vựng
            // Chữ Hàn
            // Câu
          ),
          
        ],
      )
    );
  }
}